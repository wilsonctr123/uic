---
description: Run Playwright E2E tests against the live frontend
allowed-tools:
  - Bash
  - Read
argument-hint: "[--headed] [--no-start]"
---

Run the Playwright E2E tests generated by UIC.

The tool will **auto-start the dev server** using `startCommand` from `uic.config.ts` if the app isn't already running, and stop it when done.

1. Resolve and run:
```bash
UIC="node $(git rev-parse --show-toplevel)/tool/dist/cli.js"
$UIC test run $ARGUMENTS
```

2. After success, report:
   - Number of tests passed / failed / skipped
   - Path to test results

3. If tests fail, report the failures with details.

4. If no tests exist, tell the user to run `/uic-test-gen` first.
